name: Build project

on: pull_request

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Setup up Node.js environment
        uses: SBence/setup-node-workflow-yarn@master
        with:
          node-version: "*"

      - name: Build project
        run: yarn tsc --noEmit
